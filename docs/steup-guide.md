## Setup-Guide

### Backend Dependencies Installation:

1. cd apps/server
2. npm install express cors dotenv
3. npm install -D nodemon


### Run the Backend (Refactored)

The backend is started via `src/server.js`, while `src/app.js` only configures the Express app.

```bash
cd apps/server
npm run dev
